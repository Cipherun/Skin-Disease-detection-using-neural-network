**Skin Disease Detection**
steps to run 
1. pip install -r requirements.txt
2. py -3.12 -m uvicorn api_backend_auth:app --reload  (in one termial)
   open another new terminal run following command)
3. py -3.12 -m streamlit run streamlit_white_portal.py.
